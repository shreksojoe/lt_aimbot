Current Issue: program skipping to press "save" button, before it inputs the order and all it's details. 
Skips straight from checkboxes to Save

Issue fixed with faulty if statment. (discarded all rows that weren't above 8 in length)

Still goes to the plus button, figure out what code that it is and remove it
Plus coords: (834, 353) (also 885, 364) for product button
key value == "Coordinates"
                        ^
                        |
                        L___ plural

Pulled form order_instructions function in csv_no_plus.py
order_array passed into order_instructions
order_array passed from main to csv_no_plus.py
order_array comes from order.json in instructions


these mofo's just got archived
    {"Name": "Plus Button", "Coordinates": [834, 353]},
    {"Name": "Product Button", "Coordinates":[885, 364]},
    {"Order Amount": ""},
    {"Name": "OK Button", "Coordinates":[703, 252]},
    {"Window": "Editing Ticket"},


Now it doesn't type in product no. and goes stoopid with typing in json values 
Likely because "Coordinates" Valuse were still in the json, and that had been removed from csv_no_plus.py

Doesn't stop any more, but skipping the duplication and not typing in the right values
csv_rows likely faulty

